{
  "name": "keliss-invoice-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Polls HubSpot for finalised invoices flagged Branded PDF = Generate, renders a KELISS-branded PDF with Playwright, uploads it to File Manager, attaches it to the invoice and deal, and writes the URL + status back.",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "preview": "node preview.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.47.0"
  }
}
