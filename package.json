{
  "name": "keliss-invoice-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Renders a KELISS-branded PDF the moment a HubSpot invoice is finalised or flagged Branded PDF = Generate (webhook-driven, with a safety-net search), uploads it to File Manager, attaches it to the invoice and deal, and writes the URL + status back.",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "preview": "node preview.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "wrangler": "^4.135.0"
  }
}
